import 'package:flutter/widgets.dart';

/// Root navigator key, shared by the router (and any code that needs to
/// navigate from outside the widget tree).
final rootNavigatorKey = GlobalKey<NavigatorState>();
